[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -m "not integration"

markers =
    integration: Tests that require production data (local media files). Run with: pytest -m integration
